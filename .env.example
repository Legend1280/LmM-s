# LLM Gateway API - Environment Variables Configuration

# ============================================================================
# API Configuration
# ============================================================================
API_TITLE="LLM Gateway API"
API_VERSION="1.0.0"

# Server Configuration
HOST="0.0.0.0"
PORT=8000
WORKERS=4

# ============================================================================
# Authentication
# ============================================================================
# Comma-separated list of valid API keys
# Leave empty for development mode (no authentication)
API_KEYS="your-api-key-1,your-api-key-2,your-api-key-3"

# ============================================================================
# Model Backend URLs
# ============================================================================

# Llama 7B API endpoint (OpenAI-compatible)
# Example: http://localhost:8001/v1 or your hosted endpoint
LLAMA_API_URL="http://localhost:8001/v1"

# Gemini API endpoint (OpenAI-compatible)
# Example: https://generativelanguage.googleapis.com/v1beta/openai/
GEMINI_API_URL="https://generativelanguage.googleapis.com/v1beta/openai/"

# OpenAI API Key (for GPT-4 Mini)
OPENAI_API_KEY="sk-your-openai-api-key-here"

# ============================================================================
# Redis Configuration (for batch jobs)
# ============================================================================
REDIS_HOST="localhost"
REDIS_PORT=6379
REDIS_DB=0

# ============================================================================
# Database Configuration (for logging)
# ============================================================================
# PostgreSQL connection string
# Example: postgresql://user:password@localhost:5432/llm_gateway
DATABASE_URL="postgresql://user:password@localhost:5432/llm_gateway"

# ============================================================================
# Logging
# ============================================================================
LOG_LEVEL="INFO"

# ============================================================================
# Notes
# ============================================================================
# 1. For development, you can leave API_KEYS empty to disable authentication
# 2. Model backend URLs should point to OpenAI-compatible API endpoints
# 3. Redis is optional - the system will fall back to in-memory storage
# 4. Database is optional - logs will go to stdout if not configured
